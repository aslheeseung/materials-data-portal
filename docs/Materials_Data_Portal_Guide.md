# Materials Data Portal - 사용 가이드 및 개발 요약

---

## 📋 목차

1. [프로젝트 개요](#프로젝트-개요)
2. [시스템 구조](#시스템-구조)
3. [설치 및 실행](#설치-및-실행)
4. [주요 기능](#주요-기능)
5. [Lab 페이지 사용법](#lab-페이지-사용법)
6. [API 참조](#api-참조)
7. [개발 이력](#개발-이력)

---

## 🎯 프로젝트 개요

**Materials Data Portal**은 재료 과학 연구자를 위한 AI 기반 데이터 포털입니다.

### 핵심 가치
- **Materials Project** 데이터베이스 검색
- **AI Agent** 기반 자동화된 분석
- **MLIP (UPET)** 통합으로 에너지 계산
- **다중 에이전트 시스템**으로 복잡한 쿼리 처리

### 배포 URL
- **Production**: https://materials-data-portal.vercel.app
- **GitHub**: https://github.com/aslheeseung/materials-data-portal

---

## 🏗️ 시스템 구조

```
┌─────────────────────────────────────────────────────────────┐
│                     Materials Data Portal                    │
├─────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐  │
│  │  Web Portal │  │  MCP Server │  │   Python Server     │  │
│  │  (Next.js)  │  │  (Node.js)  │  │   (FastAPI+MLIP)    │  │
│  └──────┬──────┘  └──────┬──────┘  └──────────┬──────────┘  │
│         │                │                     │             │
│         ▼                ▼                     ▼             │
│  ┌─────────────────────────────────────────────────────────┐│
│  │                    Multi-Agent System                    ││
│  │  ┌───────────────────────────────────────────────────┐  ││
│  │  │                  Master Agent                      │  ││
│  │  │         (계획 수립 및 결과 종합)                    │  ││
│  │  └───────────────────┬───────────────────────────────┘  ││
│  │           ┌──────────┼──────────┐                        ││
│  │           ▼          ▼          ▼                        ││
│  │  ┌─────────────┐ ┌─────────┐ ┌─────────────┐            ││
│  │  │  Database   │ │Synthesis│ │  Compute    │            ││
│  │  │   Agent     │ │  Agent  │ │   Agent     │            ││
│  │  └─────────────┘ └─────────┘ └─────────────┘            ││
│  └─────────────────────────────────────────────────────────┘│
│                                                              │
└─────────────────────────────────────────────────────────────┘
```

### 에이전트 역할

| Agent | 역할 | 주요 기능 |
|-------|------|----------|
| **Master** | 오케스트레이션 | 사용자 쿼리 분석, 작업 분배, 결과 종합 |
| **Database** | 데이터 검색 | Materials Project 검색, 상세 조회 |
| **Synthesis** | 합성 정보 | 레시피 검색, 생성, 전구체/온도 필터 |
| **Compute** | 계산 분석 | MLIP 에너지, Phase Diagram, 구조 최적화 |

---

## 🚀 설치 및 실행

### 사전 요구사항

- Node.js 18+
- Python 3.10+
- Materials Project API Key
- OpenAI API Key

### 1. 웹 포털 설치

```bash
# 저장소 클론
git clone https://github.com/aslheeseung/materials-data-portal.git
cd materials-data-portal

# 의존성 설치
npm install

# 환경 변수 설정
# .env.local 파일 생성
OPENAI_API_KEY=sk-...
MP_API_KEY=...

# 개발 서버 실행
npm run dev
```

### 2. MCP 서버 설치

```bash
cd mcp-server
npm install
npm run build
```

### 3. Python 계산 서버 설치

```bash
cd python-server

# 가상환경 생성 (권장)
python -m venv venv
venv\Scripts\activate  # Windows
# source venv/bin/activate  # Mac/Linux

# 의존성 설치
pip install -r requirements.txt

# UPET MLIP 설치 (선택사항)
pip install upet

# 서버 실행
$env:MP_API_KEY="your-api-key"  # PowerShell
python -m uvicorn main:app --port 8000
```

### 4. 전체 시스템 실행

```bash
# 터미널 1: 웹 포털
npm run dev  # http://localhost:3000

# 터미널 2: Python 서버
cd python-server
python -m uvicorn main:app --port 8000
```

---

## 📱 주요 기능

### 1. 홈 페이지 (/)

- AI 챗봇 기반 재료 검색
- 자연어로 질문하면 재료 정보 제공
- 예: "LiCoO2의 밴드갭 알려줘"

### 2. Agent 페이지 (/agent)

- 단일 AI Agent 인터페이스
- 복합적인 재료 과학 질문 처리

### 3. 합성 데이터베이스 (/synthesis)

- 합성 레시피 브라우징
- 필터 기반 검색

### 4. Lab 페이지 (/lab) ⭐ NEW

멀티 에이전트 시스템의 핵심 인터페이스

---

## 🧪 Lab 페이지 사용법

### 모드 선택

| 모드 | 설명 | 사용 사례 |
|------|------|----------|
| **Master** | 멀티 에이전트 협업 | 복잡한 종합 질문 |
| **Single** | 단일 에이전트 직접 사용 | 특정 영역 집중 쿼리 |
| **Research** | 연구 모드 | 체계적 스크리닝 |

### Master 모드 예시

```
입력: "LiCoO2 배터리 양극재로 적합한지 분석해줘"

→ Master Agent: 작업 분배
  → Database Agent: LiCoO2 데이터 검색
  → Synthesis Agent: 합성 방법 조회
  → Compute Agent: 구조 분석
→ Master Agent: 결과 종합 및 보고
```

### Single 모드 예시

**Database Agent:**
```
"Li-Co-O 시스템 안정한 재료 찾아줘"
"mp-22526 상세 정보"
"band gap 1.5-2.5 eV 재료"
```

**Synthesis Agent:**
```
"LiCoO2 합성 레시피"
"sol-gel 방법으로 LiNiO2 만들어줘"
"Li2CO3 전구체 사용하는 레시피"
```

**Compute Agent:**
```
"mp-149 에너지 계산해줘"
"mp-22526 formation energy"
"Li-Fe-O phase diagram"
"mp-149 구조 최적화"
```

### Research 모드 예시

```
입력: "21개 전이금속에서 3원계 합금 후보 추천해줘"

→ 조합 생성: C(21,3) = 1330개
→ 체계적 스크리닝
→ 안정성, 응용 가능성 분석
→ 상위 후보 추천 + 근거 설명
```

---

## 📡 API 참조

### Lab API

#### POST /api/lab
Master Agent 멀티 에이전트 오케스트레이션

```json
{
  "message": "사용자 질문",
  "language": "ko"
}
```

#### POST /api/lab/single
Single Agent 직접 호출

```json
{
  "message": "질문",
  "agent": "database|synthesis|compute",
  "language": "ko"
}
```

#### POST /api/lab/research
Research 모드 스크리닝

```json
{
  "message": "연구 쿼리",
  "language": "ko"
}
```

### Compute Server API (Python)

| Endpoint | Method | 설명 |
|----------|--------|------|
| `/health` | GET | 서버 상태 확인 |
| `/analyze/material` | POST | 재료 구조 분석 |
| `/phase-diagram` | POST | Phase diagram 생성 |
| `/element/{symbol}` | GET | 원소 정보 |
| `/mlip/status` | GET | MLIP 상태 확인 |
| `/mlip/energy` | POST | 에너지 계산 |
| `/mlip/formation-energy` | POST | Formation energy |
| `/mlip/relax` | POST | 구조 최적화 |

---

## 📜 개발 이력

### Phase 1: 기초 구축
- Next.js 14 프로젝트 설정
- Materials Project API 연동
- 기본 검색 UI 구현

### Phase 2: MCP 서버
- MCP 프로토콜 서버 구현
- Claude Code 연동
- 5가지 검색 도구 개발

### Phase 3: AI Agent
- OpenAI GPT-4 연동
- 자연어 질의 처리
- AI 챗봇 인터페이스

### Phase 4: 합성 데이터베이스
- 합성 레시피 데이터 구축
- 검색/필터 기능
- 레시피 자동 생성

### Phase 5: Multi-Agent Lab ⭐
- Master Agent 오케스트레이션
- Database/Synthesis/Compute 전문 에이전트
- 에이전트 간 대화 시각화
- Master/Single/Research 모드

### Phase 6: MLIP 통합
- UPET (Universal Potential) 연동
- 에너지 계산 (single-point)
- Formation energy 계산
- 구조 최적화 (relaxation)

### Phase 7: 배포 최적화
- Vercel 배포 문제 해결
- OpenAI lazy initialization
- 환경 분리 (.vercelignore)

---

## 🔧 문제 해결

### "계산 서버 연결 오류"
→ Python 서버 실행 확인: `python -m uvicorn main:app --port 8000`

### "MLIP 사용 불가"
→ UPET 설치: `pip install upet`

### "MP API 401 오류"
→ API 키 설정: `$env:MP_API_KEY="your-key"`

### Vercel 배포 실패
→ `.vercelignore`에 python-server 포함 확인

---

## 📞 연락처

- **GitHub Issues**: https://github.com/aslheeseung/materials-data-portal/issues

---

*Last Updated: 2026-02-01*
*Developed with Claude Code (Claude Opus 4.5)*
